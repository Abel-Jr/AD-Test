FROM winlogbeat-*
| WHERE event.code == "4776" //AND winlog.event_data.ErrorCode IN ("0xC0000064", "0xC000006A")
| STATS distinct_accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY host.ip
| WHERE distinct_accounts >= 8